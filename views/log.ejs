<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <title>Login Success</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #3498db; /* Background color */
        }

        .message {
            text-align: center;
            background-color: #2ecc71; /* Message background color */
            color: #fff; /* Text color */
            padding: 20px;
            border-radius: 5px;
            animation: fade-in 1s ease-out; /* Animation */
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
</head>
<body>
    <div class="message">
        <h1><%=logStatus%></h1>
        <a href="/"><button type="button" class="btn btn-primary" name="button">Back To Home!</button></a>
        <!-- You can add more content or links here if needed -->
    </div>
</body>
</html>
